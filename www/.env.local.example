# Site Configuration
# Copy this file to .env.local and update the values for your environment

# The canonical URL of your site (required for OpenGraph metadata)
# This should be the full URL where your site is deployed (e.g., https://donation-widget.example.com)
# For local development, you can leave this unset to use the default fallback (http://localhost:3000)
NEXT_PUBLIC_SITE_URL=https://donation-widget.example.com
